branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ] || [ "$branch" = "develop" ]; then
  echo "Direct push to '$branch' is not allowed. Use a feature branch and create a PR."
  exit 1
fi

pnpm lint && pnpm typecheck && pnpm test
